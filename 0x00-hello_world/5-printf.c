#include <stdio.h>

/**
 * Main: the entry point
 *
 * Return: Always return 0 if programme runs successfully
 */

int main(void)
{
    printf("with proper grammar, but the outcome is a piece of art,\n");
    return 0;
}
