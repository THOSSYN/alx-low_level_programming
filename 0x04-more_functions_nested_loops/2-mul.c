#include<stdio.h>

/**
 *mul - find the product of two integers.
 *@a: first variable
 *@b: second variable
 *Return: result of a multiply b
 */

int mul(int a, int b)
{
	int result;
	result = (a * b);
	return (result);
}
